name: Benchmarks

on:
  workflow_dispatch:
    inputs:
      versions:
        description: "Space-separated list of versions (e.g. '1.6.1 1.7.0')"
        required: true
        default: "1.6.1 1.7.0"

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

permissions:
  contents: write
  actions: write

jobs:
  bench:
    runs-on: ubuntu-24.04

    steps:
      - uses: actions/checkout@v6
        with:
          ref: master
          fetch-depth: 0

      - uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: 11

      - name: Run benchmarks
        run: |
          chmod +x benchmarks/run.sh
          ./benchmarks/run.sh ${{ github.event.inputs.versions }}

      - name: Commit results
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add docs/assets/benchmarks/
          git diff --staged --quiet || git commit -m "bench: results for ${{ github.event.inputs.versions }}"
          git push

      - name: Trigger pages deployment
        run: gh workflow run pages.yml --ref master
        env:
          GH_TOKEN: ${{ github.token }}
