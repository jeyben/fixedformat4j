  Features
------------------------------------------
  - cannot create instances of inner class records
  - Use Record annotation to append padding chars in the end of the line.
  - Support the ability to write the same instructions into multible fields in a string.
    like an annotation containing multibel annotations:
      @Retention(RetentionPolicy.RUNTIME)
      @Target({ElementType.METHOD})
      public @interface Fields {
        public abstract Field[] value();
      }


  Site
------------------------------------------
  - Include reports
  - Include http://cobertura.sourceforge.net/
  - document

  Test Coverage
------------------------------------------
  - Some corners are cut. Find them and chase them down


